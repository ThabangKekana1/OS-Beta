import { DashboardResourcesRoute } from "@/components/resources/DashboardResourcesRoute";

export default function AdminResourcesPage() {
  return (
    <DashboardResourcesRoute
      eyebrow="Admin Resources"
      title="Download sales collateral and onboarding templates."
      description="Use this library to share the latest company profile, brochure, and EOI template with prospects."
    />
  );
}
