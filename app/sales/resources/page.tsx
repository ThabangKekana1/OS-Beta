import { DashboardResourcesRoute } from "@/components/resources/DashboardResourcesRoute";

export default function SalesResourcesPage() {
  return (
    <DashboardResourcesRoute
      eyebrow="Sales Resources"
      title="Download and share core 1OS sales material."
      description="Access the latest company profile, brochure, and EOI template directly from the sales dashboard."
    />
  );
}
